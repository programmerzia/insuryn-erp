# Decisions log

Review decisions applied to the Phase 0 kernel. Each entry: the decision, its status, and where it lives
in code and tests. Design references are to `docs/design-package-v1.md`.

| ID | Decision | Status | Where |
|---|---|---|---|
| D-01 | Balance trigger `journal_must_balance` fires immediately (`DEFERRABLE INITIALLY IMMEDIATE`), not deferred as sketched in §2.2. Lines must be inserted before status → `posted`. | Applied (slice 0.0) | Comment in `database/migrations/2026_09_12_000003_accounting_triggers_and_rls.php`; `JournalWriter` inserts draft → lines → status flip; `InvariantsTest` "never posts an unbalanced journal". |
| D-02 | Session-level tenant setting (`set_config(..., false)`) accepted for MVP. Deploy with direct connections or session-mode pooling only; transaction-mode PgBouncer unsupported until SET LOCAL per request (LATER). | Applied (slice 0.0) | `CONTEXT.md` "Deployment constraints", `README.md` "Deployment", `TenantContext` docblock; reconnect re-applies the tenant (`PlatformServiceProvider`, `ResolveTenantTest`). |
| D-03 | Events are inserted directly as `queued`; `received` stays in the enum for a future pre-validation stage. | Applied (slice 0.0) | `Domain/Enums/EventStatus.php` docblock; `SubmitAccountingEvent`. |
| D-04 | New event type `CLAIM_RESERVE_ADJUSTED` (lines as `CLAIM_RESERVED`, amount `payload.delta`, negative delta flips sides). Fixture 06b now uses it with `{delta: -5000000}` and unchanged expected lines. `CLAIM_RESERVED` accepts positive amounts only. | Applied (slice 0.0) | `resources/posting-rules/CLAIM_RESERVE_ADJUSTED.default.json`; `tests/Fixtures/golden/06b_claim_reserve_adjusted_decrease.json` (renamed from `06b_claim_reserve_decrease.json`); `Application/Posting/EventPayloadValidator.php` (`AMOUNT_NOT_POSITIVE`); `DatabaseRuleViolationTest` (feature). |
| D-05 | Payroll rule stays summary-level for MVP; per-employee lines are Phase 2. | Applied (slice 0.0) | `_comment` in `resources/posting-rules/PAYROLL_POSTED.default.json` (JSON has no comments; the rule loader ignores unknown keys). |
| D-06 | OPEN #2 (VAT refund on cancellation) not resolved: rule unchanged. `product_versions.tax_profile.refund_tax_on_cancellation` (bool, default **true**, ASSUMPTION); the cancellation mapper sends `tax_reversal = 0` when false and the engine drops zero lines. | Applied (accepted in 0.0; flag implemented in slice 1A.2, cancellation mapper in slice 1A.3) | See `docs/PROGRESS.md` slices 1A.2 / 1A.3. |
| D-07 | Outbox relay loops over `tenants` (platform table, not RLS-protected, allowed by §8.6.5) and relays inside `TenantContext::run` per tenant. No bypass role. | Applied (slice 0.0) | `Infrastructure/Jobs/OutboxRelayJob.php`; `OutboxRelayTest` (lost dispatch posted; relay twice → one journal). Found while testing: `ShouldBeUnique` locks at dispatch and never expires, so a lost dispatch blocked re-delivery forever — replaced by `WithoutOverlapping` keyed by event id (taken at execution). |
| D-08 | `role_permissions`, `user_roles`, `approval_decisions`, `reconciliation_exceptions`, `period_close_tasks` get `tenant_id uuid not null`, backfilled from the parent, with the standard RLS policy. Non-negotiable #6 has no exceptions. | Applied (slice 0.0) | `database/migrations/2026_09_13_000001_add_tenant_to_child_tables.php` (additive migration instead of editing the shipped `TENANT_TABLES` constant; backfill runs per tenant under RLS); `Platform/Database/RowLevelSecurity.php`; `TenantIsolationTest` child-table case; `SchemaInvariantsTest` asserts every non-global table has tenant_id + forced RLS + policy. |
| D-09 | `User` matches `users`: UUIDv7 key, `BelongsToTenant`, non-incrementing, still `Authenticatable`. `ResolveTenant` runs before auth. | Applied (slice 0.0) | `app/Models/User.php`, `database/factories/UserFactory.php`; `UserModelTest` (tenant scoping, authenticated request, middleware order StartSession < ResolveTenant < Authenticate). |
| D-10 | Trigger rejections (`PERIOD_CLOSED`, `PERIOD_MISSING`, `UNBALANCED_JOURNAL`, `EMPTY_JOURNAL`, `IMMUTABLE_JOURNAL`, SQLSTATE 23514) mark the event `failed` with that code, no retry. `UNEXPECTED` only for anything else. | Applied (slice 0.0) | `Application/Posting/DatabaseRuleViolation.php`, `PostingEngine::post`; `DatabaseRuleViolationTest` (unit mapping + feature race: period locked after the app check). `PERIOD_MISSING` added to the list because the same trigger raises it. |
| D-11 | Reversals and manual journals require approval when an approval policy matches, and always enforce maker ≠ checker. | Applied (accepted in 0.0; SoD in slice 0.4, approvals, manual journals and reversal workflow in slice 0.5; the same pattern is reused for refunds 1A.5 and claim payments 1B.1) | See `docs/PROGRESS.md` slices 0.4 / 0.5. |
| D-12 | Distribution (docs/distribution-module-design.md) is its own business context `App\Modules\Distribution`. It depends on Platform and `Accounting\Application` only. Insurance uses Distribution's application layer (read-only contracts such as `ProducerDirectory`, and later the compensation calculator) and keeps the commission subledger and its accounting events ("Distribution feeds it"). No context uses Distribution's Domain, Infrastructure or Http. | Applied (slice D1) | `tests/Architecture/DependencyTest.php` (two Distribution rules); `app/Modules/Distribution`. |
| D-13 | Phase 1 `agents` become `producers` keeping their ids. Referencing columns keep their names (`policies.agent_id`, `receipts.collected_by_agent_id`, `agent_deposits.agent_id`, `commission_entries.agent_id`, `commission_statements.agent_id`) and `dim_agent` keeps its name; all now mean producer. Renaming them would touch every posting rule, fixture and report for no behavioural gain. Phase 1 status `inactive` maps to `suspended`. | Applied (slice D1) | Migration `2026_09_18_000001_create_distribution_producers`, `LegacyAgentBackfill`, `ProducersTest`. |
| D-14 | The compensation calculation is a pure domain function in Distribution (`CompensationCalculator`, golden fixtures in `tests/Fixtures/compensation`), wrapped by an application engine that loads terms and records compliance exceptions. The Insurance commission subledger stays the owner of `commission_entries` and their accounting events, and calls the engine through application DTOs only (`CompensationRequest` → `CompensationOutcome`/`CommissionAward`). The cap is checked per trigger on the sum of rates (basis points), which is Σ commission ≤ cap × basis for every premium, and so for the policy. | Applied (slice D5) | `app/Modules/Distribution/Domain/Compensation`, `CompensationEngine`, `CommissionAccrual`; `CompensationGoldenTest`, `CompensationEngineTest`. |
| D-15 | The design note's `producer_statements` is the existing `commission_statements` table, extended (split columns, `draft`, `period_end`, payout route), so there is one statement concept, and the Phase 1 per-agent approval and bank payment stay valid statements. Advances live in Distribution and post their own events; the commission subledger calls `AdvanceService` inside the statement approval. | Applied (slice D6) | Migration `2026_09_18_000006`, `CommissionStatementRun`, `AdvanceService`; `StatementRunTest`, `CommissionPayoutTest`. |
| D-16 | Sanctum's `personal_access_tokens` is created by our own migration as a tenant table (tenant_id + forced RLS, UUIDv7 id, uuid morphs) instead of the package's published migration, with a tenant-scoped token model. External portal users are `users.kind = portal`: they authenticate only by API token and are refused by the staff web sign-in. | Applied (slice D9) | Migration `2026_09_18_000008_create_producer_portal`, `Platform\Authentication\PersonalAccessToken`, `AuthenticationServiceProvider`; `ProducerPortalTest`, `TenantIsolationEveryTableTest`, `SchemaInvariantsTest`. |
| D-17 | Onboarding adds no business rules: the setup wizard (`App\Http\Setup`, a composition controller) calls the service that owns each piece of data, and only data that had no creation path outside seeders (legal entity and branches, fiscal year, subledger controls, a tax rate) gets a thin setup service in its own context. The chart-of-accounts template is a CSV imported through the existing COA import. Wizard progress is a tenant table, not user preferences, because setup is shared by the people who own its steps. | Applied (session S1) | `app/Http/Setup`, `Platform\Setup`, `Accounting\Application\Setup`, `Platform\Tax\TaxRateSetup`; `SetupWizardTest`. |
| D-18 | `product_classes` (Phase 3 design §1, listed without a tenant) is a global catalogue like `account_roles`: code, English and Bangla names, life/non-life, status. The four MVP classes (motor, fire, marine_cargo, misc) are `active`; marine_hull, engineering, health and life are seeded `later` so their codes are reserved and cannot be used yet. Inserted by migration and by `ProductClassesSeeder` (tests truncate global tables). A class must match the product's life/non-life class. | Applied (slice R1) | Migration `2026_09_20_000001_product_classes_and_coverages`, `ProductClassesSeeder`, `ProductCatalogue::ratingTerms`; `SchemaInvariantsTest` (global table, stated reason), `ProductRatingTermsTest`. |
| D-19 | Phase 3 coverages are rows in the new tenant table `coverages` (code, EN/BN names, mandatory, basis, rating rule reference, limit and deductible rules). The Phase 1 `product_versions.coverages` JSON list is left untouched for compatibility (the API and screens still send it) and is not read by rating. Rating fields and coverages change only while no policy uses the version; a version in use is immutable (add a new version). | Applied (slice R1) | `ProductCatalogue::addVersion` (`coverage_definitions`), `addCoverage`, `configureRating`; `ProductRatingTermsTest`. |
| D-20 | Rating lives in `App\Modules\Insurance\Rating` (it depends on products: classes, risk schemas, coverages) and uses Platform only — not `Accounting\Application`: the posting-rule evaluator is not reused as an instance, rating builds its own Symfony ExpressionLanguage with `RatingFunctions`, so posting-rule behaviour and fixtures are untouched. `Product` does not use the Rating module (it checks `rating_plans` as a table). Rate units are integers: `rate_pct` = basis points of a percent, `rate_pm` = hundredths of a per mille, flat = minor units; bands are half-open. | Applied (slice R2) | `app/Modules/Insurance/Rating`, `RateValueType`, `RatingMath`; `RatingExpressionsTest`, arch test "no floats in rating". |
| D-21 | The design's plan statuses draft/active/retired gain `approved` between draft and active, for the draft → approve → activate lifecycle: approval (maker ≠ checker) freezes the plan, activation puts it in force and is where the one-active-plan-per-class-per-date INVARIANT is checked. Activating over an active plan is refused unless the caller asks to supersede, which ends the current plan the day the new one starts. Approved, active and retired plans are immutable (service and trigger); a change is a new version. | Applied (slice R2) | `RatingPlanStatus`, `RatingPlanService`, migration `2026_09_20_000002`; `RatingPlanLifecycleTest`. |
| D-25 | Documents are one generic Platform service, `DocumentStore`, over a tenant table `stored_documents` that names its object by audit subject type and id (no foreign key, so any context can attach). Rows are append-only (trigger, like audit_events) and files are content-addressed per tenant (`<tenant>/<aa>/<sha256>` on a private disk, never overwritten); a correction is a new document. Access to a document is the access to its object, checked by the business controller; Platform only refuses by type, size and tenant. `document.attached` and `document.downloaded` are audited on the object. Phase 3 generated PDFs point at this table. | Applied (fix F2) | Migration `2026_09_19_000020_create_stored_documents`, `Platform\Documents\DocumentStore`, `App\Http\Pages\ObjectDocuments`; `ObjectDocumentsTest`, `TenantIsolationEveryTableTest`. |
| D-26 | An approval policy step may name a role as well as a permission (`{permission, role}`). When it names a role, holding that role (in any scope) is the authority to decide the step, as in design §7.2 "CFO: approve above thresholds" and §7.3 "> 500,000 → Finance Manager + CFO" — neither role holds `claim.approve`. The step's permission stays the approval duty: it is checked by `SodGuard` and written to the audit trail. Steps without a role behave exactly as before (the decider needs the permission). Policies are effective-dated and never rewritten: changing one in force ends it the day the successor starts; overlapping policies (same object type, amount band and days) are refused, so "strictest wins" is never needed for policies made on the screen. | Applied (fix F3) | `ApprovalService` (docblock, `assertMayDecide`, `holdsRole`), `ApprovalInboxQuery`, `ApprovalPolicyService`; `ApprovalLimitsTest`. |
| D-27 | Account role mappings are remapped from a date, never edited: the mapping in force that day gets `effective_to` = that date (not included, as `PostingContextLoader` reads it, so its last day is the day before) and the new mapping starts that day, keeping the old end date. A remap is refused when the role already has a mapping starting on or after the date (no overlap, no overwriting a later change) and when journals of the entity and book were already posted with the role on or after the date (history is not rewritten; choose a later date or post a correction). The setup wizard's chart-of-accounts step commits the import only when every role the posting rules in force use has an account. | Applied (fix F4) | `AccountRoleMappingService`, `ChartOfAccountsSetup::import`; `AccountRolesTest`, `SetupWizardTest`. |
| D-28 | XLSX exports use a small dependency-free writer (`Platform\Exports\XlsxWriter`: one sheet of inline-string cells, five XML parts zipped with ext-zip) instead of a spreadsheet package. The repository has no spreadsheet library, the agency register needs one plain sheet, and a package (openspout, PhpSpreadsheet) would add a dependency for that alone; replace the writer with a package if exports need styles, formulas or streaming very large sheets. The register export stays one service (`IdraRegisterExport`, CSV and XLSX from the same table) behind one controller action, routed on the API and on the web for the producers queue. | Applied (F6) | `app/Modules/Platform/Exports/XlsxWriter.php`, `IdraRegisterExport::xlsx`, `LicenceController::register`, `routes/web.php`; `AgencyRegisterExportTest`. |
| D-30 | Quotations, proposals and cover notes are new-business modules of the Insurance context, one per document (`Insurance\Quotation`, `Insurance\Underwriting`, `Insurance\CoverNote`), separate from `Insurance\Policy`: the Phase 1 policy `quote` status stays as it is (a policy row before issue) and R7 issues policies from approved proposals. A quotation stores the full `RatingResult::toArray()` of its last rating plus the plan code, version and premium columns for queues; once issued, the database refuses any change to its terms (trigger `QUOTATION_FROZEN`, transitions `QUOTATION_TRANSITION`), so a later tariff never alters it (design INVARIANT "rating_result frozen", applied at quotation level). Live rating (`POST /quotations/rate`) writes nothing. | Applied (slice R4) | `app/Modules/Insurance/Quotation`, migration `2026_09_24_000001`; `QuotationWorkbenchTest`. |
| D-31 | Underwriting referrals use the approval engine (object type `proposal_referral`), so maker ≠ checker, role steps, the approvals inbox and audit work as for claims. Because the authority comes from underwriting limits (per role, class and sum insured) rather than amount bands, an approval may now carry its own steps (`approvals.steps`, `policy_id` optional, CHECK one of them; `ApprovalService::requestWithSteps`); the engine and the inbox read steps from the approval when present, otherwise from the policy, and everything else is unchanged. An approval policy for `proposal_referral` configured on the Approval limits screen still wins when it matches. The handler adds what the engine cannot know: `underwriting.decide` on the branch, SoD on the proposal's own audit trail and the decider's own limit. | Applied (slice R5) | Migration `2026_09_24_000002`, `ApprovalService`, `ApprovalInboxQuery`, `ProposalService`, `UnderwritingDecisions`, `ProposalReferralApprovalHandler`; `ProposalUnderwritingTest`. |
| D-32 | A stored rating result can be rated again on its own terms: `RatingEngine::rerate(result, ?ManualLoading)` loads the plan by the id in the result (any non-draft status — approved plans are immutable), the product version, date, risk inputs and coverages recorded in it, so without a loading it reproduces the result exactly even after the tariff changed. A manual loading is a step the calculator applies itself (code `manual_loading`, labels "Special terms loading N%" / Bangla) after the plan's premium steps and the product minimum, before rounding and duties; the inputs hash includes it only when present, so every existing hash and golden fixture is unchanged. A counter-offer re-rates the quotation's frozen result, never a previous loading. R7 uses `rerate` for endorsements on the original plan version. | Applied (slice R5) | `RatingEngine::rerate`, `RatingCalculator`, `RatingRequest::manualLoading`, `ManualLoading`; `RatingRerateTest`. |
| D-33 | A cover note posts no accounting (design §2 step 3 ASSUMPTION, product flag `recognise_at` default `policy`). Recognising premium at cover note would need its own event and posting rules and a way to hand the recognised premium over to the policy's issue event without breaking the INVARIANT that a policy's rating result and issue posting are frozen at issue — `POLICY_ISSUED` cannot simply be reused, because the policy would then post the same premium again. Until that is designed, issuing a cover note for a product version with `recognise_at = cover_note` is refused (`RECOGNITION_AT_COVER_NOTE_NOT_SUPPORTED`); this is a known gap. R7 supersedes cover notes inside the policy issue transaction (`CoverNoteService::supersedeForProposal`). | Applied (slice R6) | `CoverNoteService`, migration `2026_09_24_000003`; `CoverNotesTest`. |
| D-34 | Generated PDFs are printed by the Chrome binary in headless mode through Symfony Process (`ChromePdfRenderer` behind the `PdfRenderer` interface), not spatie/browsershot: browsershot drives Chromium through Node's puppeteer, which would add a Node dependency and a second Chromium download to every install and CI run, while Chrome is already on the machine (and on GitHub's ubuntu runners). The design DECISION it serves — headless Chromium, not DOMPDF, for Bangla shaping — is unchanged. Flags: `--headless=new --no-sandbox --disable-gpu --no-pdf-header-footer --print-to-pdf`, a throwaway profile per render, network requests sent to a closed local proxy. Fonts (IBM Plex Sans, Noto Sans Bengali; OFL) are self-hosted in `resources/fonts/documents` and embedded as data URIs, so glyphs never depend on installed fonts. `--blink-settings=scriptEnabled=false` makes headless Chrome write no PDF, so scripts are refused by the template guard instead. Binary `erp.documents.chrome_binary` (env `ERP_CHROME_BINARY`, default `/usr/bin/google-chrome`); CI sets it. | Applied (slice R8) | `Platform\Documents\Rendering\ChromePdfRenderer`, `DocumentFonts`, `PlatformServiceProvider`, `.github/workflows/ci.yml`; `ChromePdfRendererTest` (real Bangla render, not skipped). |
| D-35 | Documents are generated by one Platform service, `DocumentGenerator`, from tenant templates (`document_templates`, versioned, one active per code, class and locale; active and retired versions immutable by trigger) and data providers: a business context implements `DocumentDataProvider` (object type, template codes, subject — the page object the PDF belongs to, number, product class, authorization scope — and the variable bag) and tags it in its service provider, so Platform never uses Insurance and a new document is one class. Every template receives the same variable bag (`DocumentVariables`), so bodies are portable between documents. The PDF is stored with a narrowly scoped `DocumentStore::storeGenerated` — the same disk, hash, limits and append-only row as `attach`, but audited as `document.generated` with the template and version instead of `document.attached` by a person. `generated_documents` is append-only by trigger; regeneration takes the next version per (object type, object id, template code) under an advisory lock, in one transaction with the stored document and the audit row. | Applied (slice R8) | `Platform\Documents\Generation\*`, `Platform\Documents\Templates\*`, `DocumentStore::storeGenerated`, migration `2026_09_26_000001`, `InsuranceServiceProvider` (tags); `DocumentGenerationTest`, `DocumentTemplatesTest`, `TenantIsolationEveryTableTest`. |
| D-36 | The tariff editor (Phase 3 design §6) adds no rating rules: its controllers (`Rating\Http\Controllers\TariffsPageController`, `DutiesPageController`) call RatingPlanService and DutyBook, which keep the permission, draft-only, maker ≠ checker and one-active-plan checks. The only application additions are what the screen needs: `RatingPlanService::replaceRows` and `updateStep` (draft-only, same checks and audit as adding), a read-only `RatingPlanDirectory`, and `RatingPlanDiff`, a pure comparison of two plan definitions. Rates cross HTTP as the stored integers (D-20); converting what people type ("2.25‰", "10%", "2,500.00") happens on the client on the digits of the text, never through a float, and the server validates integers. The plan page has its own tab set (Overview · Tables · Steps · Duties · Diff · Timeline · Audit) instead of extending the shared ObjectPage, whose tabs are fixed to transactions, accounting and documents; it reuses Timeline and AuditList over ObjectHistory. | Applied (slice R10a) | `app/Modules/Insurance/Rating/Http/Controllers`, `RatingPlanDirectory`, `RatingPlanDiff`, `resources/js/lib/rating.ts`, `resources/js/pages/rating/plans`; `TariffEditorTest`, `RatingPlanDiffTest`, `rating.test.ts`. |
| D-37 | Stamp duty of a rated policy is posted on its own account role, `stamp_duty_payable`, not carried inside VAT: POLICY_ISSUED and POLICY_ENDORSED get rule version 2 (`POLICY_ISSUED.default.v2.json`, `POLICY_ENDORSED.default.v2.json`; version 1 stays on record) with a fourth line `payload.stamp_duty ?? 0` / `payload.stamp_duty_delta ?? 0`, so every event without stamp duty — all products without a rating plan, and events queued before the change — posts exactly as version 1 (the zero line is dropped before its role is looked up). Carrying stamp duty in `tax` would have overstated the VAT liability and refunded stamp duty pro rata on cancellation. Policies and policy transactions hold the amount (`stamp_duty_minor`, `stamp_duty_delta_minor`); the premium CHECK is gross = net + tax + stamp duty. The role is in the global catalogue (migration and `AccountRolesSeeder`), the demo chart (2155 Stamp Duty Payable), the chart-of-accounts template and the captions (EN/BN). Existing tenants must map it (Accounting → Account roles shows it unmapped) before issuing a rated policy with stamp duty. Golden fixtures `01d_policy_issued_with_stamp_duty`, `01e_policy_endorsed_rerated_decrease`; the earlier POLICY_ISSUED/ENDORSED fixtures pass unchanged on version 2. | Applied (slice R7) | `resources/posting-rules/*.v2.json`, `PolicyAccountingEvents`, `RatedPremium`, migration `2026_09_28_000001`; `GoldenRulesTest`, `PolicyIssueFromProposalTest`, `AccountingCaptionsTest`. |
| D-38 | The design's `policies.previous_policy_id` (renewal chain) is the existing `renewal_of_policy_id` (slice 1A.3, used by `PolicyLifecycle::renew`, `PolicyYear`, persistency and the licence rule for renewals); no second column. R9 renewals set it when they issue the renewal. | Applied (slice R7, no change) | `policies.renewal_of_policy_id`, `Policy` model docblock. |
| D-39 | Issuing from a proposal and re-rated endorsements are methods of `PolicyLifecycle` (`issueFromProposal`, `rateEndorsement`, `endorseRisk`), which uses the Underwriting and CoverNote application services (`ProposalService::approvedForIssue` / `markIssued`, `CoverNoteService::supersedeForProposal`) and `RatingEngine` inside its own transaction; those modules never use Policy (Underwriting reads the `policies` table for the duplicate check only). A product version is rated when it has a product class (A-115). An endorsement's re-rating is stored on its policy transaction (`rating_result`, `rating_basis`), never on the policy, whose issue rating is frozen by trigger; `RatingEngine::rerateWith(result, inputs, loading, coverages)` rates new inputs on a stored result's plan version, product version and date (`rerate` is it with the stored inputs). The demo seeders sell rated products through quotation → proposal → policy (`Database\Seeders\DemoNewBusiness`), with the clock set to each sale's story day, instead of keeping the demo products unrated: the stories now exercise the real path and their bank lines follow the rated premiums. | Applied (slice R7) | `PolicyLifecycle`, `EndorsementRating`, `RatingEngine::rerateWith`, `DemoNewBusiness`, `PartADemoSeeder`, `DemoBusinessSeeder`, `DistributionDemoSeeder`; `PolicyIssueFromProposalTest`, `PartADemoTest`. |
| D-40 | Renewals are their own Insurance module, `Insurance\Renewal` (expiry register, renewal quotations, notices, queue), using the Quotation, Product and Platform application services; Policy never uses it. A renewal is linked on the quotation (`quotations.renewal_of_policy_id`, frozen once issued, one open renewal quotation per policy) rather than on the proposal or a new column on policies, because the quotation is what the customer accepts and the proposal already names its quotation: `PolicyLifecycle::issueFromProposal` reads the link, sets the new policy's `renewal_of_policy_id` (D-38), skips the new-business licence check, moves the expiring policy `active \| expired → renewed` in the same transaction (audited `policy.renewed`) and dispatches `PolicyRenewed`, which the register listens to. Renewal quotations are made by `QuotationService::offerRenewal` — the same rating, numbering, freeze and audit as `issue` — with no creating user when the nightly run offers them (`created_by` null only for a renewal, CHECK; audited as the system). `UnderwritingRules` treats a renewal as not new business and not a duplicate of the policy it renews. | Applied (slice R9) | Migration `2026_09_29_000001`, `QuotationService::offerRenewal` / `declineRenewal`, `PolicyLifecycle::issueFromProposal`, `PolicyRenewed`, `UnderwritingRules`, `app/Modules/Insurance/Renewal`; `RenewalsTest`. |
| D-41 | Notifications are a Platform service: `NotificationChannel` (adapter interface), `LogNotificationChannel` (the MVP adapter: writes the application log), `Notifier` (sends through every channel enabled in `erp.notifications.channels` — email and SMS, each naming its adapter — and records each message once per idempotency key and channel in the tenant table `notifications`, a refusal recorded as `failed`). Gateway adapters (SSL Wireless, Twilio, an email provider) are LATER and register one class each. Documents produced by a scheduled run have no person to authorize: `DocumentGenerator::generateBySystem` skips the permission check, leaves `generated_documents.rendered_by` null and audits `document.generated` as the system (`DocumentStore::storeGenerated` takes a nullable actor); everything else about generation is unchanged. | Applied (slice R9) | `Platform\Notifications\*`, `DocumentGenerator::generateBySystem`, `DocumentStore::storeGenerated`, migration `2026_09_29_000001`; `RenewalsTest`, `TenantIsolationEveryTableTest`. |
| D-42 | The expiry register is a stored projection (`expiry_register`, one row per policy) rather than a query, because it carries decisions that are not on the policy — the renewal quotation offered, the quote problem, why the customer did not renew — and the notices hang off it. It is rebuilt idempotently (insert … on conflict, only open rows refreshed; closing rules in `ExpiryRegister`), nightly by `RenewalRunJob` (per tenant, D-07) and when the queue is opened. Statuses `upcoming \| renewal_offered \| renewed \| lapsed \| not_renewed`: `lapsed` is closed by the system (expired without renewal, policy cancelled or lapsed), `not_renewed` is a person's record with a reason. The reports read policies and join the register, so the expiry register report can be run as at any date and conversion counts renewals issued outside the register. Conversion is an integer basis-point ratio (half up). | Applied (slice R9) | `ExpiryRegister`, `ExpiryRegisterStatus`, `RenewalReasons`, `ExpiryRegisterReportQuery`, `RenewalConversionQuery`; `RenewalsTest`. |
| D-43 | A screen area opens for a holder of any of its permissions in any scope (`PermissionChecker::authorizeArea`, returning an `AreaReach`: tenant-wide, entity ids, branch ids), rather than only tenant-wide (`authorizeAny` without a scope) or by the bare "holds it anywhere" check the quotation and proposal pages had (`permissionsOf`). List queries are limited with `AreaReach::constrain(query, entity column, branch column)`, and a record page checks the record's own branch with `authorizeAny(…, AuthorizationScope::branch(…))` — 403, the existing convention for a missing permission, not 404. Actions keep their own per-branch checks in the application services. | Applied (fix G2) | `AreaReach`, `PermissionChecker`; policy, receipt, claim, quotation and proposal page controllers; `BranchScopedPagesTest`. |
| D-46 | Property tests over a stateful lifecycle are written against a reference model, not by re-reading the services: `Tests\Feature\Claims\ClaimLifecycleModel` states the claim rules (design §4.6–§4.8, §5.5 and the 1B.1 interpretations), predicts accept or the refusal's reason code for every drawn operation and applies accepted ones; the test asserts the services agree, that a refusal writes nothing, and the invariants against the database after every operation. Randomness comes from a tiny in-repo `Tests\Support\Property\SeededGenerator` on PHP's own `Random\Engine\Xoshiro256StarStar` (no Composer dependency, isolated from `mt_rand` so application code cannot shift a sequence); run i uses seed + i, so one failing run replays alone. No shrinking: a failure prints the seed, the replay command and the operations so far. One tenant per test with a fresh claim per run (reseeding a tenant per run would cost about a second each); the default size (100 runs, up to 20 operations, fixed seed 20260914, about 45 s) is deterministic for CI and larger or other seeds are run through `PROPERTY_RUNS`, `PROPERTY_STEPS`, `PROPERTY_SEED`. With 50 runs or more the test also requires every operation and every refusal to have been reached, so a generator change cannot quietly stop exercising a path. | Applied (slice 2.0d) | `tests/Feature/Claims/ClaimReservePropertyTest.php`, `ClaimLifecycleModel`, `ClaimOperation`, `tests/Support/Property/SeededGenerator.php`. |
| D-48 | The E2E happy path (design §9.1) is a plain Node script on `playwright-core` with a small assert helper (`tests/e2e/lib.mjs`), not `@playwright/test`: the repo already has `playwright-core` and drives the system Chrome (`CHROME`, default `/usr/bin/google-chrome`; the ubuntu-24.04 runners ship it), so nothing is downloaded and it runs offline; one sequential story does not need a test runner's parallelism, fixtures or reporters. It fails on regressions — assertions on every journal preview (balanced, expected lines and amounts), record statuses, posted journals, the trial balance and the failed-events queue, plus any uncaught page error or 5xx — exits non-zero, and leaves screenshots, Playwright traces (`npx playwright-core show-trace`) and a console log in `storage/e2e/`. Selectors are by role, label and field id, like `scripts/flow-audit.mjs`, whose patterns are copied rather than imported (the audit measures; the test asserts). | Applied (slice 2.0c) | `tests/e2e/happy-path.mjs`, `tests/e2e/lib.mjs`, `npm run test:e2e` / `test:e2e:run`. |
| D-49 | The E2E job gets its own runner, `scripts/e2e.sh`, used identically locally and in CI: fresh migrate + seed + `php artisan erp:demo` in the explicitly named database, frontend build when the manifest is missing, `php artisan serve` (4 PHP server workers) and a queue worker started in the background and stopped by their PIDs (with their child processes), readiness = the demo tenant's `/login` answering 200 on 127.0.0.1 with the tenant `Host` header, then the test's exit status. CI runs it as a separate `e2e` job (own Postgres service, PHP 8.4, Node 24) so a browser failure is reported apart from Pest, and uploads `storage/e2e/` and the Laravel log on failure. | Applied (slice 2.0c) | `scripts/e2e.sh`, `.github/workflows/ci.yml` job `e2e`. |
| D-53 | Every document number from a branch-scoped sequence carries the branch code: receipts, claims and agent deposits get `{prefix}-{branch}-{fy}-{seq}` in `erp.numbering.formats` like policies, quotations, proposals and cover notes, and `DocumentNumberer` uses that format for any type with none configured (`{branch}` left out for an entity-level sequence, so commission statements are unchanged). Chosen over one entity-wide sequence per type because that would restart at 000001 and collide with numbers existing tenants already issued; a branch-coded number never equals a branch-less one, so running sequences continue and issued numbers are never rewritten. The customer's format choice is CQ-E5 (A-150). | Applied (fix G5) | `config/erp.php` `numbering.formats`, `DocumentNumberer::DEFAULT_FORMAT`; `BranchNumberingTest`, `DocumentNumbererTest`. |
| D-54 | Business dates follow the legal entity's time zone (CQ-H2 decided by the product owner): one Platform service, `App\Modules\Platform\Tenancy\BusinessClock` (`today(?entityId)`, `now(?entityId)`, `timezone(?entityId)`), reads `legal_entities.timezone` (new column, default Asia/Dhaka, existing entities take their tenant's `timezone`; set on the setup wizard's company step). Every business date — default dates on forms and page controllers (X1/X2), value, approval and payment dates, "as of" defaults on reports and the trial balance, the current fiscal period, renewal T-45, dunning, grace and lapse, quotation, cover note and licence expiry, the nightly jobs — comes from it; a business date is returned as midnight UTC of the local date so it compares like dates read from the database. Technical timestamps (created_at, audit `occurred_at`, decided_at, reservation TTLs) stay UTC. The nightly jobs are scheduled on the default zone and read each company's today inside the tenant loop; date inputs count `t` from the server-shared `businessToday`. An architecture test forbids reading the application clock as a date in every module's Http, Application and Infrastructure code and in app/Http. | Applied (slice 2.1b) | `BusinessClock`, migration `2026_10_01_000211_add_timezone_to_legal_entities`, `CompanySetup`, `routes/console.php`, `HandleInertiaRequests`, `DateInput.vue`; `tests/Architecture/BusinessClockTest.php`, `Platform/BusinessClockTest`. |
| D-55 | Documents pending at month end block the lock, never the soft lock (CQ-C4 decided): the kernel contract `Accounting\Application\Contracts\PendingCloseDocuments` (container-tagged; the kernel lists manual journals and reversals awaiting approval and accounting events not posted, Insurance lists claim payments not yet paid and refunds not yet released) feeds `PendingDocumentsQuery`. The close checklist (Run and Index) lists them; the trial balance task soft-locks with a warning; `FiscalPeriodService::lock` refuses `PERIOD_HAS_PENDING_DOCUMENTS` (details.pending in JSON) until each is approved, rejected or — a manual journal pending approval — moved by its approver to the first day of the next open period (`ManualJournalService::moveToNextPeriod`, audited `journal.moved_to_next_period`; `ApprovalService::assertMayDecideCurrentStep` decides who "its approver" is). No override. | Applied (slice 2.1b) | `PendingDocumentsQuery`, `KernelPendingDocuments`, `ClaimPaymentsPendingAtClose`, `RefundsPendingAtClose`, `FiscalPeriodService::assertReadyToLock`, `ClosePageController`; `Close/PendingDocumentsAtCloseTest`. |
| D-56 | Early lock (CQ-C5 decided): on the business clock, a period may be soft-locked from its last day (`PERIOD_LAST_DAY_NOT_REACHED` before) and locked once it has ended (`PERIOD_NOT_ENDED` before). The one exception is a CFO, read conservatively as a holder of `periods.reopen` (only the CFO template holds it), who locks early with a mandatory written reason (`EARLY_LOCK_REASON_REQUIRED`), audited on `period.locked` with the reason and `early_lock: true`. The soft-lock rule has no exception, so an early lock is possible from the last day at the earliest. The period lock task's note is the early-lock reason. | Applied (slice 2.1b) | `FiscalPeriodService::softLock` / `lock`, `PeriodCloseService`, `ClosePageController::run` (`lock.early`), `close/Run.vue`; `Close/EarlyLockTest`. |
| D-59 | Branch filtering (D-43) applies to every screen that lists branch-bound records, not only record pages: the screen opens with `authorizeArea` and each list is limited with `AreaReach::constrain` on the table that carries the record's entity and branch (receipts for suspense and cheques, the policy for dunning notices, refundable policies and installments, the producer's branch for agent cash). Read queries shared with reports and close checks take an optional `AreaReach` that defaults to everywhere, so reports, close tasks and the API stay tenant-wide. A Home queue or badge is limited to the reach of the area of the page its rows open (policies, receipts, quotations or claims), so a queue never lists a row the user would get 403 on; a user holding the role but none of that area's permissions sees the queue empty. Lookups and search limit branch-bound kinds (policies, installments, claims, receipts) and leave parties unfiltered (A-160). Printing and downloading a quotation or cover note checks the object's branch like the other document routes. | Applied (follow-up H1) | `AreaReach`, `CollectionsPageController`, `SuspenseQuery`, `RefundableQuery`, `ChequeRegisterQuery`, `AgentCashPositionQuery`, `ReferralsPageController`, `CoverNotesPageController`, `RenewalsPageController`, `GeneratedDocumentsController`, `WorkQueues`, `GlobalSearchQuery`, `LookupController`; `BranchScopedQueuesTest`. |
| D-60 | Risk schema problems are worded at the app layer, not in the domain: `RiskInputsInvalid` keeps its codes per field (the API contract) and carries the `RiskSchema` that refused them; `App\Http\Feedback\RiskProblems` turns them into sentences with the field labels in the user's `locale` (English or Bangla) — per field (`risk_inputs.<key>` for browser forms, `fields` in JSON next to the unchanged `errors` codes) and as a one-line summary. One exception render in `bootstrap/app.php` applies it to every path, so a new screen that validates risk inputs needs no code of its own. Reason messages for people stay in `ReasonMessages`; a unit test scans app/ for every reason code thrown and requires it to be worded there or thrown with a sentence free of ids, minor units, codes and keys, so a new refusal cannot reach a form unreadable. | Applied (follow-up H2) | `RiskInputsInvalid`, `RiskSchema::validate`, `App\Http\Feedback\RiskProblems`, `ReasonMessages::covers`, `bootstrap/app.php`, `PreviewJournal`; `tests/Unit/Feedback/ReasonMessagesCoverageTest.php`, `QuotationWorkbenchTest`, `ProposalRiskDetailsTest`, `PolicyIssueFromProposalTest`. |
| D-61 | Design §5.5 gains two claim transitions, derived from the money rather than a new status: a claim that is `reserved` with a paid payment (only reachable by reopening) closes like a paid claim — nothing unsettled, the reserve above what was approved released with the normal CLAIM_CLOSED journal, so the INVARIANT Σ claims_outstanding per claim = 0 after close holds — and a recovery is accepted whenever the claim has anything paid over its life, in any status. A separate `reopened` status was not added: it would need a migration, the payment and reserve rules would have to list it everywhere `reserved` appears, and "has paid" is already what the rules mean. `ClaimService::closable` / `recoverable` hold the rule for the service and the claim page; the property test's reference model states it independently and requires both transitions to be exercised. | Applied (follow-up H3) | `ClaimService`, `ClaimReserveBook::paidMinor`, `ClaimPageController`; `ClaimsTest`, `ClaimsCommissionApprovalsPagesTest`, `ClaimReservePropertyTest`, `ClaimLifecycleModel`. |
| D-62 | The date picker is DateInput's own month grid inside reka-ui's Popover: pure functions in `lib/calendar.ts` and the `DateCalendar.vue` component. It does not use reka-ui's `DatePicker` / `Calendar` primitives, because in 2.10 they:<br>• work on `@internationalized/date` values, which the app has only as a transitive dependency and which differ from the `Y-m-d` string every form and the server use<br>• have no PageUp/PageDown, and no Shift for a year<br>• take today from the browser's time zone<br>The Popover keeps the layering: Escape closes the calendar, not the drawer around it, and focus returns to the field. Typing, the parser and the value contract are unchanged. `min` / `max` disable days, stop keyboard moves and refuse a typed date outside them. | Applied (UX U1) | `resources/js/components/forms/DateInput.vue`, `DateCalendar.vue`, `resources/js/lib/calendar.ts`; `date-picker.test.ts`. |
| D-63 | The rules for a new account live in one place, `ChartOfAccountsRules` (`checkNew`, `insertNew`). The CSV import uses them, and through it the setup wizard and the journal-line account from X10. `ChartOfAccounts` (Accounting → Chart of accounts) uses them too. X10 writes one-row CSV files instead; the screen does not, because it needs:<br>• its own messages ("Account 1010 already exists.", not "imports never update accounts")<br>• a parent chosen by id<br>• an audit per account (`account.created`), not per file (`chart_of_accounts.imported`)<br>`ChartOfAccounts` also changes existing accounts (`update`, `deactivate`, `reactivate`). It refuses with reason-coded `BusinessRuleViolation`s, and with `AccountInvalid` for field errors. Whether another context still posts to an account directly is asked through the tagged `AccountUsage` contract (Finance: `BankAccountGlUsage`), so the Accounting kernel does not read business tables. | Applied (UX U2) | `app/Modules/Accounting/Application/ChartOfAccounts/*`, `Contracts/AccountUsage.php`, `ChartOfAccountsImport`, `ChartOfAccountsPageController`, `BankAccountGlUsage`; `ChartOfAccountsScreenTest`, `ImportWizardTest`, `ManualJournalAccountCreateTest`. |
| D-65 | GA-03: the branch officer does not allocate. Design §7.3 has no rule against one person recording and allocating, but §7.2 deliberately gives `receipt.allocate` to the Branch Manager and the Accountant only (allocation moves the receivable and earns commission), so this is not a template gap. A receipt recorded by someone without `receipt.allocate` in the branch carries no allocation lines and notes the policy it was taken for (`receipts.for_policy_id`); the money sits in suspense and the branch manager allocates it from Home ("Receipts to allocate") in the workbench, where that policy comes first. Granting allocate to the officer stays a one-line template change if the customer wants allocation at the counter. | Applied (gap fix GA-03) | `ReceiptService::assertNotedPolicy`, `CollectionsPageController::create/store` (`allocateBranchIds`), `WorkQueues` `receipts_to_allocate`; `OfficerReceiptIntoSuspenseTest`. |
| D-66 | GA-07: error pages are one Inertia page (`errors/Error`) rendered from the exception handler for browser requests: `PermissionDenied` on GET directly (with the permissions it names), anything else with status 403/404/419/500/503 through `$exceptions->respond()`; inside AppLayout when the shared shell props exist (a signed-in route), otherwise a bare card. JSON/API responses and refused form posts are unchanged; a 500 keeps Laravel's debug page while APP_DEBUG is on. "Ask for access" is a mailto to up to three active users holding `platform.manage_users` or `platform.manage_roles` (there is no access-request workflow). | Applied (gap fix GA-07) | `bootstrap/app.php`, `App\Http\Feedback\ErrorPage`, `resources/js/pages/errors/Error.vue`; `ErrorPagesTest`. |
| D-67 | GA-24: a journal line caption may depend on the event as well as the account role: `resources/help/roles.<locale>.md` has a second table (Event, Role, Debit, Credit); `/help/roles` serves those under `<EVENT_TYPE>:<role>` keys next to the role captions and `captionFor(captions, role, side, event)` prefers them. Used for the unearned premium a cancellation releases. | Applied (gap fix GA-24) | `HelpContent::eventCaptions`, `resources/js/lib/captions.ts`; `CancellationSettlementTest`, `captions.test.ts`. |
| D-70 | Agent commission on premium received is earned on the net premium inside each allocation, not on the cash allocated (gap audit GA-42; product owner decision, to verify with the customer as CQ-F8). The allocation is split pro rata between net premium and VAT plus stamp duty in the proportion of the transaction that billed the installment (`PremiumNetShare`), because installments carry only a gross amount and adding net and duty columns to installments would need a backfill of every existing schedule; the proportion is exact for the issue and each premium increase, and a policy-level allocation uses the policy's net ÷ gross. The base is one setting (`erp.commission.premium_received_base`, default `net_premium`, `gross` = the earlier behaviour) rather than a field per scheme or plan, as neither model has a base field and a migration for a per-scheme base was not asked for. Clawbacks follow the entries they claw back. | Applied (gap fixes W2) | `EarnCommissionOnAllocation`, `PremiumNetShare`, `config/erp.php`; `CommissionNetPremiumBaseTest`, `CommissionTest`. |
| D-71 | The demo and template products earn premium 1/365 per day on cover (`daily_365`) instead of by calendar month (gap audit GA-44; product owner decision, to verify as CQ-D1): monthly earning gave a policy starting on 5 August nothing for August. Monthly stays a supported method per product version; the setup wizard's default is `erp.setup.earning_method`. | Applied (gap fixes W2) | `PartADemoSeeder`, `SetupPageController`, `config/erp.php`; `SetupWizardTest`, `PartADemoTest`. |
| D-72 | A recovery lowers incurred claims in the period its recovery journal is dated — when it is received — and not in the period of the loss or of the claim's closing: closed months stay closed, so the loss ratio for a past month never moves. The report shows claims incurred, recoveries and net incurred as columns, and a signed ratio as "(1,230.77)%" (gap audit GA-06). | Applied (gap fixes W2) | `LossRatioQuery`, `ReportsPageController::percentText`; `LossRatioPageTest`. |
| D-73 | The four registers kept on their own screens (suspense ageing, agent cash, commission statements, trial balance) export through the report export path of flow fix X12 (`GET /reports/{key}/export`), from tables built in `OperationalReportTables`, rather than each screen getting its own download: one path, one set of filters and defaults, one file naming (gap audit GA-34). | Applied (gap fixes W2) | `OperationalReportTables`, `ReportsPageController::index/export`; `ReportExportTest`. |
| D-74 | Tenant dimension requirements (`dimension_requirements`, design §2.1) are enforced by the posting engine on top of each rule's own `dimensions.required`, with the same `DIMENSION_MISSING` failure, so a tenant can require a dimension (class on premium and claim lines) without editing posting rules or golden fixtures (gap audit GA-47). | Applied (gap fixes W2) | `PostingEngine`, `TenantDimensionRequirements`; `DimensionRequirementsTest`. |
| D-75 | One commission payout path: statements are prepared and approved only by the monthly statement run (`CommissionStatementRun`), then paid by someone else (`CommissionPayoutService::pay`). The Phase 1 "approve an agent's payout up to a date" was removed from the service, the web (`POST /commission/statements`, `/commission/statements/{id}/pay`, `/commission/plans`) and the API (`POST /api/insurance/agents/{agent}/commission-statements`, replaced by `POST /api/insurance/commission-statements/prepare` and `…/{id}/approve`) rather than kept as a second route to the same `commission_statements`, because two workbenches let a statement be made twice from different rules and confused which screen finance uses. `/commission` stays as read-only history (statements of both runs, Phase 1 plans); the sidebar and palette open "Commission statements". Money invariants and SoD are unchanged; the Phase 1 payout tests were ported onto the run. | Applied (GA-10) | `CommissionStatementRun`, `CommissionPayoutService`, `CommissionController`, `CommissionPageController`, `StatementWorkbenchController`, `routes/web.php`, `routes/api.php`, `navigation.ts`, `commands.ts`; `CommissionPayoutTest`, `ClaimsCommissionApprovalsPagesTest`, `TenantIsolationEveryTableTest` |
| D-76 | One producer register: the Phase 1 agents screen (`/agents`, its create form and `PartyPageController::agents`) is gone and `/agents` redirects to the producers queue filtered to agents; producers (with type, channel and licence) are created only in the producers register or inline from the quote (X9). The palette's "New quote" and the Policies list open the rated quote workbench; the typed-premium form is offered as "New policy (unrated product)" only while a product without a rating plan exists. | Applied (GA-10, GA-11) | `routes/web.php`, `PartyPageController`, `navigation.ts`, `commands.ts`, `PolicyPageController::index` (`unratedProducts`), `policies/Index.vue`; `PartiesProductsPoliciesPagesTest`, `PolicyRatingScreensTest`, `palette.test.ts` |
| D-77 | A party's contact details are columns on `parties` (mobile, email, address, identity number, date of birth, contact person) validated by one value object, `PartyContact`, rather than a separate contacts table: one mobile and one address per party is what renewals, claims messages and the schedule need now, the table keeps its RLS and no new tenant table is added. The customer page is an `ObjectPage` with tabs of its own (Policies, Claims, Receipts) through two additive props, `extraTabs` and `hiddenTabs` (a party has no accounting tab), and documents on subject `party` through `DocumentStore`. | Applied (GA-17) | migration `2026_10_03_000071`, `PartyContact`, `PartyService`, `PartyPageController`, `ObjectPage.vue`, `parties/Show.vue`, `PartyContactFields.vue`, `LookupController::createCustomer`; `CustomerDetailsTest` |
| D-78 | The user's language is chosen in the user menu and kept in the preference `locale` (help, tour, captions, risk labels and printing follow it); the help panel's own English/বাংলা switch sets a separate preference, `help_locale` (null follows `locale`), so reading the panel in the other language never changes the rest of the interface. | Applied (GA-30) | `UserPreferences`, `preferences.ts`, `HelpPanel.vue`, `TopBar.vue`; `HowThisWorksTest`, `help-language.test.ts` |
| D-79 | An endorsement's additional installment carries the endorsement's number (`installments.endorsement_no`) and is named by one helper, `InstallmentLabel` (`POL-…/E1`, the A-104 endorsement number), wherever installments are listed or allocated; the confirmation after an endorsement names it and offers two next steps through an additive `also` on the flashed `next` step (a second toast action): Record receipt for an increase, Print endorsement. A manual journal's supporting vouchers are stored documents on subject `journal`, and its provisional reference is derived from its id rather than reserving a number, so the gapless JV sequence is untouched. | Applied (GA-25, GA-31) | migration `2026_10_03_000073`, `InstallmentPlanner`, `PolicyLifecycle::endorsementNo`, `InstallmentLabel`, `NextSteps::afterEndorsement`, `toasts.ts`, `Toaster.vue`, `ProvisionalReference`, `JournalPageController`; `PolicyRatingScreensTest`, `ManualJournalVoucherTest`, `next-steps.test.ts` |
| D-80 | Nightly jobs record every tenant's run in `job_runs` through the Platform `JobRunLog` (status, start and finish, who pressed "Run now", the counts the job returned or the error; per entity for dunning). The jobs share the `RunsNightly` trait: every tenant when the schedule starts them, one tenant with `forTenant()` for "Run now". The close screen reads the latest run per job. In development `php artisan dev` registers a queue worker on every queue and `schedule:work` instead of Horizon's default-queue supervisor. | Applied (GA-05) | `app/Modules/Platform/Jobs/{JobRunLog,RunsNightly}.php`, `app/Http/Close/NightlyJobs.php`, `AppServiceProvider::registerDevProcesses`; `NightlyJobsTest`. |
| D-81 | The year-end close is a kernel journal of kind `closing` written through the JournalWriter by `Accounting\Application\Close\YearEndClose`, not a posting rule: its lines depend on which income and expense accounts carry a balance, which fixed rule roles cannot express, and no business module raises it. The architecture test that limits JournalWriter to the kernel's posting path names this one class next to the posting engine, reversals and manual journals. The close task `year_end_close` exists only in the run of a fiscal year's last month, after every task that posts to income and expense and before the trial balance; the sign-off runs it again for late postings. | Applied (GA-15) | `YearEndClose`, `CloseTaskCatalogue::tasks($yearEnd)`, `tests/Architecture/DependencyTest.php`; golden fixtures `tests/Fixtures/golden/year_end/`, `YearEndCloseTest`. |
| D-82 | Accounts that are not subledger control accounts reconcile through `AccountBalanceReconciler` (a SubledgerReconciler naming its account roles, and whether GL lines without the item dimension are outside the register) rather than `subledger_controls` rows, so the unearned premium, VAT payable and stamp duty payable accounts stay ordinary accounts that manual journals may post to. They are tagged like the other reconcilers: nightly, as close tasks and in the lock's variance check. | Applied (GA-43) | `Accounting\Application\Contracts\AccountBalanceReconciler`, `ReconciliationService::measure`, `Insurance\Policy\Application\Reconciliation\*`; `CloseReconciliationTasksTest`. |
| D-83 | The close tasks added by the gap fixes take free order numbers — 7 unearned premium, 9 suspense, 10 VAT payable, 11 stamp duty payable reconciliations, 12 year-end close — which order the checklist and are not design §5.7 row numbers (7 AP/AR, 9–12 remain LATER there). Tasks that post (`CloseTaskCatalogue::POSTING_TASKS`: premium earning, year-end close) are flagged so the checklist previews their journal. | Applied (GA-43, GA-15, GA-09) | `CloseTaskCatalogue`, `ClosePageController::run`, `close/Run.vue`. |
| D-84 | The journal preview also shows journals an action writes directly: the JournalWriter raises the in-process `JournalWritten` event when it posts, and `PreviewJournal` lists those written while the action runs (manual journal approvals, approved reversals, the year-end close) before posting the recorded accounting events. The approvals inbox reads what the approver decides on through the optional `PreviewsApprovalSubject` handler interface (details, link label, the lines the final approval posts), only on the approvals screen, not in the inbox counts every page shares. | Applied (GA-04, GA-09) | `Accounting\Application\Posting\JournalWritten`, `App\Http\Preview\PreviewJournal`, `Platform\Approvals\PreviewsApprovalSubject`; `ApprovalPreviewTest`, `PostingActionPreviewsTest`. |
