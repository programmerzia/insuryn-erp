{
  "code": "COMMISSION_EARNED.default",
  "version": 1,
  "event_type": "COMMISSION_EARNED",
  "applies_to": { "product_codes": ["*"], "lob": ["*"], "channels": ["*"] },
  "effective_from": "2026-01-01",
  "effective_to": null,
  "books": ["LOCAL"],
  "dimensions": { "required": ["branch","product","policy","agent"], "inherit_from_event": true },
  "lines": [
    { "role": "commission_expense",             "side": "debit",  "amount": "pct(payload.base, payload.rate_bp)" },
    { "role": "commission_payable",             "side": "credit", "amount": "sub(pct(payload.base, payload.rate_bp), pct(pct(payload.base, payload.rate_bp), payload.withholding_bp))" },
    { "role": "commission_withholding_payable", "side": "credit", "amount": "pct(pct(payload.base, payload.rate_bp), payload.withholding_bp)" }
  ],
  "validation": ["amount > 0", "period.open"],
  "reversal": { "strategy": "mirror", "allowed_until": "period_locked" },
  "idempotency": { "key": "COMMISSION_EARNED:{commission_entry_id}" },
  "rounding": { "mode": "half_even", "residual_role": "rounding_difference" }
}
